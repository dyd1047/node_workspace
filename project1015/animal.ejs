<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        function getSubList(obj){
            // js 스크립트는 보안이 불가(HTML소스와 함께 작성되므로, 소스보기에서 원본소스가 노출)
            // var sel1=document.getElementsByName("category_id");
            location.href="/animal?category_id="+obj.value;
        }
    </script>
</head>
<body>
    <select name="categoty_id" id="" onchange="getSubList(this)">
        <option value="0">동물구분</option>
        <%for(var i=0; i<categoryArray.length; i++){%>
            <%var category=categoryArray[i];%>
            <option value="<%=category.categoty_id%>"><%=category.category_name%></option>
        <%}%>
    </select>
    <select name="animal_id" id="">
        <option value="0">동물이름</option>
        //<%for(var i=0; i<animalArray.length; i++){%>
        //    <%var animal=animalArray[i];%>
        //<option value="<%=animal.animal_id%>"><%=animal.name%></option>
        //<%}%>
    </select>
</body>
</html>